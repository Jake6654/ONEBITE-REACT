
// ES module
// 가져올 객체(함수) 와 경로 입력 
import {add, sub} from "./math.js"; 


// 랜덤 컬러 라이브러리의 기본값을 가져옴 
// 기본값(default) 는 {} 안에 객체 형태로 존재하지 않아도 됌
import randomColor from "randomcolor"; 

const colot = randomColor();
console.log(colot);



